const translations = {
    'en': {
        'admin.hero.title': 'Administrative Panel',
        'admin.hero.subtitle': 'Manage users, services and reviews',
        
        'admin.tab.users': 'Users',
        'admin.tab.services': 'Services',
        'admin.tab.reviews': 'Reviews',
        
        'admin.users.title': 'User Management',
        'admin.users.search.placeholder': 'Search users...',
        'admin.users.search.button': 'Search',
        'admin.users.table.id': 'ID',
        'admin.users.table.name': 'Name',
        'admin.users.table.email': 'Email',
        'admin.users.table.phone': 'Phone',
        'admin.users.table.role': 'Role',
        'admin.users.table.registration': 'Registration Date',
        'admin.users.table.actions': 'Actions',
        'admin.users.loading': 'Loading users...',
        'admin.users.empty': 'No users found',
        
        'admin.services.title': 'Service Management',
        'admin.services.add': '+ Add Service',
        'admin.services.search.placeholder': 'Search services...',
        'admin.services.search.button': 'Search',
        'admin.services.table.id': 'ID',
        'admin.services.table.name': 'Name',
        'admin.services.table.category': 'Category',
        'admin.services.table.price': 'Price',
        'admin.services.table.rating': 'Rating',
        'admin.services.table.popular': 'Popular',
        'admin.services.table.actions': 'Actions',
        'admin.services.loading': 'Loading services...',
        'admin.services.empty': 'No services found',
        
        'admin.reviews.title': 'Review Management',
        'admin.reviews.search.placeholder': 'Search reviews...',
        'admin.reviews.search.button': 'Search',
        'admin.reviews.table.id': 'ID',
        'admin.reviews.table.user': 'User',
        'admin.reviews.table.service': 'Service',
        'admin.reviews.table.rating': 'Rating',
        'admin.reviews.table.title': 'Title',
        'admin.reviews.table.date': 'Date',
        'admin.reviews.table.actions': 'Actions',
        'admin.reviews.loading': 'Loading reviews...',
        'admin.reviews.empty': 'No reviews found',
        
        'admin.role.user': 'User',
        'admin.role.admin': 'Administrator',
        'admin.status.yes': 'Yes',
        'admin.status.no': 'No',
        'admin.action.save': '💾',
        'admin.action.edit': '✏️',
        'admin.action.delete': '🗑️',

        'modal.addService.title': 'Add New Service',
        'modal.editService.title': 'Edit Service',
        'modal.service.title': 'Service Title:',
        'modal.service.category': 'Category:',
        'modal.service.description': 'Description:',
        'modal.service.price': 'Price:',
        'modal.service.fee': 'Commission:',
        'modal.service.rating': 'Rating:',
        'modal.service.popular': 'Popular Service:',
        'modal.service.features': 'Features (comma separated):',
        'modal.cancel': 'Cancel',
        'modal.add': 'Add',
        'modal.save': 'Save',

        'message.success': 'Operation completed successfully',
        'message.error': 'Operation failed',
        'message.confirm.delete': 'Are you sure you want to delete this item?',
        'message.access.denied': 'Access denied. Administrator rights required.',
        'message.load.error': 'Failed to load data',
        'message.required.fields': 'Please fill in all required fields'
    },
    'ru': {
        'admin.hero.title': 'Административная панель',
        'admin.hero.subtitle': 'Управление пользователями, услугами и отзывами',
        
        'admin.tab.users': 'Пользователи',
        'admin.tab.services': 'Услуги',
        'admin.tab.reviews': 'Отзывы',
        
        'admin.users.title': 'Управление пользователями',
        'admin.users.search.placeholder': 'Поиск пользователей...',
        'admin.users.search.button': 'Найти',
        'admin.users.table.id': 'ID',
        'admin.users.table.name': 'Имя',
        'admin.users.table.email': 'Email',
        'admin.users.table.phone': 'Телефон',
        'admin.users.table.role': 'Роль',
        'admin.users.table.registration': 'Дата регистрации',
        'admin.users.table.actions': 'Действия',
        'admin.users.loading': 'Загрузка пользователей...',
        'admin.users.empty': 'Пользователи не найдены',
        
        'admin.services.title': 'Управление услугами',
        'admin.services.add': '+ Добавить услугу',
        'admin.services.search.placeholder': 'Поиск услуг...',
        'admin.services.search.button': 'Найти',
        'admin.services.table.id': 'ID',
        'admin.services.table.name': 'Название',
        'admin.services.table.category': 'Категория',
        'admin.services.table.price': 'Цена',
        'admin.services.table.rating': 'Рейтинг',
        'admin.services.table.popular': 'Популярная',
        'admin.services.table.actions': 'Действия',
        'admin.services.loading': 'Загрузка услуг...',
        'admin.services.empty': 'Услуги не найдены',
        
        'admin.reviews.title': 'Управление отзывами',
        'admin.reviews.search.placeholder': 'Поиск отзывов...',
        'admin.reviews.search.button': 'Найти',
        'admin.reviews.table.id': 'ID',
        'admin.reviews.table.user': 'Пользователь',
        'admin.reviews.table.service': 'Услуга',
        'admin.reviews.table.rating': 'Оценка',
        'admin.reviews.table.title': 'Заголовок',
        'admin.reviews.table.date': 'Дата',
        'admin.reviews.table.actions': 'Действия',
        'admin.reviews.loading': 'Загрузка отзывов...',
        'admin.reviews.empty': 'Отзывы не найдены',
        
        'admin.role.user': 'Пользователь',
        'admin.role.admin': 'Администратор',
        'admin.status.yes': 'Да',
        'admin.status.no': 'Нет',
        'admin.action.save': '💾',
        'admin.action.edit': '✏️',
        'admin.action.delete': '🗑️',

        'modal.addService.title': 'Добавить новую услугу',
        'modal.editService.title': 'Редактировать услугу',
        'modal.service.title': 'Название услуги:',
        'modal.service.category': 'Категория:',
        'modal.service.description': 'Описание:',
        'modal.service.price': 'Цена:',
        'modal.service.fee': 'Комиссия:',
        'modal.service.rating': 'Рейтинг:',
        'modal.service.popular': 'Популярная услуга:',
        'modal.service.features': 'Особенности (через запятую):',
        'modal.cancel': 'Отмена',
        'modal.add': 'Добавить',
        'modal.save': 'Сохранить',

        'message.success': 'Операция выполнена успешно',
        'message.error': 'Ошибка выполнения операции',
        'message.confirm.delete': 'Вы уверены, что хотите удалить этот элемент?',
        'message.access.denied': 'Доступ запрещен. Требуются права администратора.',
        'message.load.error': 'Не удалось загрузить данные',
        'message.required.fields': 'Пожалуйста, заполните все обязательные поля'
    }
};

class AdminManager {
    constructor() {
        this.currentTab = 'users';
        this.users = [];
        this.services = [];
        this.reviews = [];
        this.filteredUsers = [];
        this.filteredServices = [];
        this.filteredReviews = [];
        this.currentLang = localStorage.getItem('language') || 'ru';
    }

    async init() {
        const currentUser = JSON.parse(localStorage.getItem('currentUser') || 'null');
        if (!currentUser || currentUser.role !== 'admin') {
            this.showError(translations[this.currentLang]['message.access.denied']);
            window.location.href = 'home.html';
            return;
        }

        this.setupTabs();
        await this.loadAllData();
        this.setupEventListeners();
        this.applyTranslations();
        this.setupLanguageObserver();
    }

    setupTabs() {
        const tabBtns = document.querySelectorAll('.tab-btn');
        tabBtns.forEach(btn => {
            btn.addEventListener('click', () => {
                const tab = btn.dataset.tab;
                this.switchTab(tab);
            });
        });
    }

    switchTab(tab) {
        this.currentTab = tab;
        
        document.querySelectorAll('.tab-btn').forEach(btn => {
            btn.classList.remove('active');
        });
        document.querySelector(`[data-tab="${tab}"]`).classList.add('active');

        document.querySelectorAll('.tab-content').forEach(content => {
            content.classList.remove('active');
        });
        document.getElementById(`${tab}-tab`).classList.add('active');

        switch(tab) {
            case 'users':
                this.renderUsers();
                break;
            case 'services':
                this.renderServices();
                break;
            case 'reviews':
                this.renderReviews();
                break;
        }
    }

    async loadAllData() {
        try {
            const [usersResponse, servicesResponse, reviewsResponse] = await Promise.all([
                fetch('http://localhost:3000/users'),
                fetch('http://localhost:3000/services'),
                fetch('http://localhost:3000/reviews')
            ]);

            if (!usersResponse.ok || !servicesResponse.ok || !reviewsResponse.ok) {
                throw new Error(translations[this.currentLang]['message.load.error']);
            }

            this.users = await usersResponse.json();
            this.services = await servicesResponse.json();
            this.reviews = await reviewsResponse.json();

            this.filteredUsers = [...this.users];
            this.filteredServices = [...this.services];
            this.filteredReviews = [...this.reviews];

            this.renderUsers();
            this.renderServices();
            this.renderReviews();

        } catch (error) {
            console.error('Error loading data:', error);
            this.showError(translations[this.currentLang]['message.load.error']);
        }
    }

    renderUsers() {
        const tbody = document.getElementById('usersTableBody');
        const t = translations[this.currentLang];
        
        if (this.filteredUsers.length === 0) {
            tbody.innerHTML = `<tr><td colspan="7" class="loading">${t['admin.users.empty']}</td></tr>`;
            return;
        }

        tbody.innerHTML = this.filteredUsers.map(user => `
            <tr>
                <td>${user.id}</td>
                <td>${user.firstname} ${user.lastname}</td>
                <td>${user.email}</td>
                <td>${user.phone}</td>
                <td>
                    <select class="role-select" data-user-id="${user.id}">
                        <option value="user" ${user.role === 'user' ? 'selected' : ''}>${t['admin.role.user']}</option>
                        <option value="admin" ${user.role === 'admin' ? 'selected' : ''}>${t['admin.role.admin']}</option>
                    </select>
                </td>
                <td>${new Date(user.registrationDate).toLocaleDateString()}</td>
                <td class="action-buttons">
                    <button class="btn-save" onclick="saveUserRole('${user.id}')" title="${t['admin.action.save']}">💾</button>
                </td>
            </tr>
        `).join('');

        document.querySelectorAll('.role-select').forEach(select => {
            select.addEventListener('change', (e) => {
                const userId = e.target.dataset.userId;
                const newRole = e.target.value;
                this.updateUserRole(userId, newRole);
            });
        });
    }

    async updateUserRole(userId, newRole) {
        const t = translations[this.currentLang];
        
        try {
            const response = await fetch(`http://localhost:3000/users/${userId}`, {
                method: 'PATCH',
                headers: {
                    'Content-Type': 'application/json'
                },
                body: JSON.stringify({ role: newRole })
            });

            if (response.ok) {
                this.showSuccess(t['message.success']);

                const userIndex = this.users.findIndex(u => u.id === userId);
                if (userIndex !== -1) {
                    this.users[userIndex].role = newRole;
                }
            } else {
                throw new Error(t['message.error']);
            }
        } catch (error) {
            console.error('Error updating role:', error);
            this.showError(t['message.error']);
            this.renderUsers();
        }
    }

    searchUsers() {
        const query = document.getElementById('usersSearch').value.toLowerCase();
        this.filteredUsers = this.users.filter(user =>
            user.firstname.toLowerCase().includes(query) ||
            user.lastname.toLowerCase().includes(query) ||
            user.email.toLowerCase().includes(query) ||
            user.phone.toLowerCase().includes(query)
        );
        this.renderUsers();
    }

    renderServices() {
        const tbody = document.getElementById('servicesTableBody');
        const t = translations[this.currentLang];
        
        if (this.filteredServices.length === 0) {
            tbody.innerHTML = `<tr><td colspan="7" class="loading">${t['admin.services.empty']}</td></tr>`;
            return;
        }

        tbody.innerHTML = this.filteredServices.map(service => `
            <tr>
                <td>${service.id}</td>
                <td>${service.title}</td>
                <td>${service.category}</td>
                <td>${service.price}</td>
                <td>${service.rating}</td>
                <td>${service.isPopular ? '✅' : '❌'}</td>
                <td class="action-buttons">
                    <button class="btn-edit" onclick="showEditServiceModal('${service.id}')" title="${t['admin.action.edit']}">✏️</button>
                    <button class="btn-delete" onclick="deleteService('${service.id}')" title="${t['admin.action.delete']}">🗑️</button>
                </td>
            </tr>
        `).join('');
    }

    searchServices() {
        const query = document.getElementById('servicesSearch').value.toLowerCase();
        this.filteredServices = this.services.filter(service =>
            service.title.toLowerCase().includes(query) ||
            service.category.toLowerCase().includes(query)
        );
        this.renderServices();
    }

    async deleteService(serviceId) {
        const t = translations[this.currentLang];
        
        if (!confirm(t['message.confirm.delete'])) return;

        try {
            const response = await fetch(`http://localhost:3000/services/${serviceId}`, {
                method: 'DELETE'
            });

            if (response.ok) {
                this.showSuccess(t['message.success']);
                this.services = this.services.filter(s => s.id !== serviceId);
                this.filteredServices = this.filteredServices.filter(s => s.id !== serviceId);
                this.renderServices();
            } else {
                throw new Error(t['message.error']);
            }
        } catch (error) {
            console.error('Error deleting service:', error);
            this.showError(t['message.error']);
        }
    }

    renderReviews() {
        const tbody = document.getElementById('reviewsTableBody');
        const t = translations[this.currentLang];
        
        if (this.filteredReviews.length === 0) {
            tbody.innerHTML = `<tr><td colspan="7" class="loading">${t['admin.reviews.empty']}</td></tr>`;
            return;
        }

        tbody.innerHTML = this.filteredReviews.map(review => `
            <tr>
                <td>${review.id}</td>
                <td>${review.userName}</td>
                <td>${review.category}</td>
                <td>${'★'.repeat(review.rating)}${'☆'.repeat(5 - review.rating)}</td>
                <td title="${review.title}">${review.title.length > 30 ? review.title.substring(0, 30) + '...' : review.title}</td>
                <td>${new Date(review.date).toLocaleDateString()}</td>
                <td class="action-buttons">
                    <button class="btn-delete" onclick="deleteReview('${review.id}')" title="${t['admin.action.delete']}">🗑️</button>
                </td>
            </tr>
        `).join('');
    }

    searchAdminReviews() {
        const query = document.getElementById('reviewsSearch').value.toLowerCase();
        this.filteredReviews = this.reviews.filter(review =>
            review.userName.toLowerCase().includes(query) ||
            review.title.toLowerCase().includes(query) ||
            review.text.toLowerCase().includes(query)
        );
        this.renderReviews();
    }

    async deleteReview(reviewId) {
        const t = translations[this.currentLang];
        
        if (!confirm(t['message.confirm.delete'])) return;

        try {
            const response = await fetch(`http://localhost:3000/reviews/${reviewId}`, {
                method: 'DELETE'
            });

            if (response.ok) {
                this.showSuccess(t['message.success']);
                this.reviews = this.reviews.filter(r => r.id !== reviewId);
                this.filteredReviews = this.filteredReviews.filter(r => r.id !== reviewId);
                this.renderReviews();
            } else {
                throw new Error(t['message.error']);
            }
        } catch (error) {
            console.error('Error deleting review:', error);
            this.showError(t['message.error']);
        }
    }

    showAddServiceModal() {
        const t = translations[this.currentLang];
        
        const modalHTML = `
            <div class="modal-overlay" id="add-service-modal">
                <div class="modal">
                    <div class="modal-header">
                        <h2 data-i18n="modal.addService.title">Добавить новую услугу</h2>
                        <button class="modal-close">&times;</button>
                    </div>
                    <div class="modal-body">
                        <form id="add-service-form">
                            <div class="form-group">
                                <label for="service-title" data-i18n="modal.service.title">Название услуги:</label>
                                <input type="text" id="service-title" required>
                            </div>
                            <div class="form-group">
                                <label for="service-category" data-i18n="modal.service.category">Категория:</label>
                                <select id="service-category" required>
                                    <option value="Invoices">Invoices</option>
                                    <option value="Quick Checkout">Quick Checkout</option>
                                    <option value="API">API</option>
                                </select>
                            </div>
                            <div class="form-group">
                                <label for="service-description" data-i18n="modal.service.description">Описание:</label>
                                <textarea id="service-description" required rows="3"></textarea>
                            </div>
                            <div class="form-group">
                                <label for="service-price" data-i18n="modal.service.price">Цена:</label>
                                <input type="text" id="service-price" value="Бесплатно" required>
                            </div>
                            <div class="form-group">
                                <label for="service-fee" data-i18n="modal.service.fee">Комиссия:</label>
                                <input type="text" id="service-fee" value="1%" required>
                            </div>
                            <div class="form-group">
                                <label for="service-rating" data-i18n="modal.service.rating">Рейтинг:</label>
                                <input type="number" id="service-rating" min="0" max="5" step="0.1" value="4.5" required>
                            </div>
                            <div class="form-group">
                                <label for="service-popular" data-i18n="modal.service.popular">Популярная услуга:</label>
                                <input type="checkbox" id="service-popular">
                            </div>
                            <div class="form-group">
                                <label data-i18n="modal.service.features">Особенности (через запятую):</label>
                                <textarea id="service-features" rows="3" placeholder="Без подписок, Учет налогов..."></textarea>
                            </div>
                        </form>
                    </div>
                    <div class="modal-footer">
                        <button class="cancel-btn" onclick="closeModal('add-service-modal')" data-i18n="modal.cancel">Отмена</button>
                        <button class="confirm-btn" onclick="addService()" data-i18n="modal.add">Добавить</button>
                    </div>
                </div>
            </div>
        `;

        document.body.insertAdjacentHTML('beforeend', modalHTML);
        this.setupModal('add-service-modal');
        this.applyTranslationsToModal('add-service-modal');
    }

    showEditServiceModal(serviceId) {
        const service = this.services.find(s => s.id === serviceId);
        if (!service) return;

        const t = translations[this.currentLang];
        
        const modalHTML = `
            <div class="modal-overlay" id="edit-service-modal">
                <div class="modal">
                    <div class="modal-header">
                        <h2 data-i18n="modal.editService.title">Редактировать услугу</h2>
                        <button class="modal-close">&times;</button>
                    </div>
                    <div class="modal-body">
                        <form id="edit-service-form">
                            <div class="form-group">
                                <label for="edit-service-title" data-i18n="modal.service.title">Название услуги:</label>
                                <input type="text" id="edit-service-title" value="${service.title}" required>
                            </div>
                            <div class="form-group">
                                <label for="edit-service-category" data-i18n="modal.service.category">Категория:</label>
                                <select id="edit-service-category" required>
                                    <option value="Invoices" ${service.category === 'Invoices' ? 'selected' : ''}>Invoices</option>
                                    <option value="Quick Checkout" ${service.category === 'Quick Checkout' ? 'selected' : ''}>Quick Checkout</option>
                                    <option value="API" ${service.category === 'API' ? 'selected' : ''}>API</option>
                                </select>
                            </div>
                            <div class="form-group">
                                <label for="edit-service-description" data-i18n="modal.service.description">Описание:</label>
                                <textarea id="edit-service-description" required rows="3">${service.shortDescription}</textarea>
                            </div>
                            <div class="form-group">
                                <label for="edit-service-price" data-i18n="modal.service.price">Цена:</label>
                                <input type="text" id="edit-service-price" value="${service.price}" required>
                            </div>
                            <div class="form-group">
                                <label for="edit-service-fee" data-i18n="modal.service.fee">Комиссия:</label>
                                <input type="text" id="edit-service-fee" value="${service.transactionFee}" required>
                            </div>
                            <div class="form-group">
                                <label for="edit-service-rating" data-i18n="modal.service.rating">Рейтинг:</label>
                                <input type="number" id="edit-service-rating" min="0" max="5" step="0.1" value="${service.rating}" required>
                            </div>
                            <div class="form-group">
                                <label for="edit-service-popular" data-i18n="modal.service.popular">Популярная услуга:</label>
                                <input type="checkbox" id="edit-service-popular" ${service.isPopular ? 'checked' : ''}>
                            </div>
                            <div class="form-group">
                                <label data-i18n="modal.service.features">Особенности (через запятую):</label>
                                <textarea id="edit-service-features" rows="3">${service.features.join(', ')}</textarea>
                            </div>
                        </form>
                    </div>
                    <div class="modal-footer">
                        <button class="cancel-btn" onclick="closeModal('edit-service-modal')" data-i18n="modal.cancel">Отмена</button>
                        <button class="confirm-btn" onclick="updateService('${serviceId}')" data-i18n="modal.save">Сохранить</button>
                    </div>
                </div>
            </div>
        `;

        document.body.insertAdjacentHTML('beforeend', modalHTML);
        this.setupModal('edit-service-modal');
        this.applyTranslationsToModal('edit-service-modal');
    }

    setupModal(modalId) {
        const modal = document.getElementById(modalId);
        const closeBtn = modal.querySelector('.modal-close');
        
        closeBtn.addEventListener('click', () => this.closeModal(modalId));
        modal.addEventListener('click', (e) => {
            if (e.target === modal) this.closeModal(modalId);
        });
    }

    closeModal(modalId) {
        const modal = document.getElementById(modalId);
        if (modal) modal.remove();
    }

    applyTranslations() {
        const t = translations[this.currentLang];
        if (!t) return;

        const elements = document.querySelectorAll('[data-i18n]');
        elements.forEach(element => {
            const key = element.getAttribute('data-i18n');
            if (t[key]) {
                element.textContent = t[key];
            }
        });

        const placeholders = document.querySelectorAll('[data-i18n-placeholder]');
        placeholders.forEach(element => {
            const key = element.getAttribute('data-i18n-placeholder');
            if (t[key]) {
                element.placeholder = t[key];
            }
        });

        const tableHeaders = document.querySelectorAll('th[data-i18n]');
        tableHeaders.forEach(th => {
            const key = th.getAttribute('data-i18n');
            if (t[key]) {
                th.textContent = t[key];
            }
        });
    }

    applyTranslationsToModal(modalId) {
        const modal = document.getElementById(modalId);
        if (!modal) return;

        const t = translations[this.currentLang];
        const elements = modal.querySelectorAll('[data-i18n]');
        
        elements.forEach(element => {
            const key = element.getAttribute('data-i18n');
            if (t[key]) {
                element.textContent = t[key];
            }
        });
    }

    setupLanguageObserver() {
        document.addEventListener('languageChanged', () => {
            this.currentLang = localStorage.getItem('language') || 'ru';
            this.applyTranslations();
            this.renderUsers();
            this.renderServices();
            this.renderReviews();
        });
    }

    showSuccess(message) {
        alert('✅ ' + message);
    }

    showError(message) {
        alert('❌ ' + message);
    }

    setupEventListeners() {
        document.getElementById('usersSearch')?.addEventListener('keypress', (e) => {
            if (e.key === 'Enter') this.searchUsers();
        });
        
        document.getElementById('servicesSearch')?.addEventListener('keypress', (e) => {
            if (e.key === 'Enter') this.searchServices();
        });
        
        document.getElementById('reviewsSearch')?.addEventListener('keypress', (e) => {
            if (e.key === 'Enter') this.searchAdminReviews();
        });
    }
}

const adminManager = new AdminManager();

async function saveUserRole(userId) {
    await adminManager.updateUserRole(userId);
}

function showAddServiceModal() {
    adminManager.showAddServiceModal();
}

function showEditServiceModal(serviceId) {
    adminManager.showEditServiceModal(serviceId);
}

async function addService() {
    const t = translations[adminManager.currentLang];
    const title = document.getElementById('service-title').value;
    const category = document.getElementById('service-category').value;
    const description = document.getElementById('service-description').value;
    const price = document.getElementById('service-price').value;
    const fee = document.getElementById('service-fee').value;
    const rating = parseFloat(document.getElementById('service-rating').value);
    const isPopular = document.getElementById('service-popular').checked;
    const features = document.getElementById('service-features').value.split(',').map(f => f.trim());

    if (!title || !category || !description) {
        alert(t['message.required.fields']);
        return;
    }

    try {
        const response = await fetch('http://localhost:3000/services', {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json'
            },
            body: JSON.stringify({
                title,
                category,
                shortDescription: description,
                fullDescription: description,
                price,
                transactionFee: fee,
                rating,
                reviewsCount: 0,
                icon: '../img/default-service.png',
                isPopular,
                features
            })
        });

        if (response.ok) {
            adminManager.closeModal('add-service-modal');
            adminManager.showSuccess(t['message.success']);
            await adminManager.loadAllData();
        } else {
            throw new Error(t['message.error']);
        }
    } catch (error) {
        console.error('Error adding service:', error);
        adminManager.showError(t['message.error']);
    }
}

async function updateService(serviceId) {
    const t = translations[adminManager.currentLang];
    const title = document.getElementById('edit-service-title').value;
    const category = document.getElementById('edit-service-category').value;
    const description = document.getElementById('edit-service-description').value;
    const price = document.getElementById('edit-service-price').value;
    const fee = document.getElementById('edit-service-fee').value;
    const rating = parseFloat(document.getElementById('edit-service-rating').value);
    const isPopular = document.getElementById('edit-service-popular').checked;
    const features = document.getElementById('edit-service-features').value.split(',').map(f => f.trim());

    try {
        const response = await fetch(`http://localhost:3000/services/${serviceId}`, {
            method: 'PATCH',
            headers: {
                'Content-Type': 'application/json'
            },
            body: JSON.stringify({
                title,
                category,
                shortDescription: description,
                fullDescription: description,
                price,
                transactionFee: fee,
                rating,
                isPopular,
                features
            })
        });

        if (response.ok) {
            adminManager.closeModal('edit-service-modal');
            adminManager.showSuccess(t['message.success']);
            await adminManager.loadAllData();
        } else {
            throw new Error(t['message.error']);
        }
    } catch (error) {
        console.error('Error updating service:', error);
        adminManager.showError(t['message.error']);
    }
}

function deleteService(serviceId) {
    adminManager.deleteService(serviceId);
}

function deleteReview(reviewId) {
    adminManager.deleteReview(reviewId);
}

function searchUsers() {
    adminManager.searchUsers();
}

function searchServices() {
    adminManager.searchServices();
}

function searchAdminReviews() {
    adminManager.searchAdminReviews();
}

document.addEventListener('DOMContentLoaded', () => {
    adminManager.init();
});